env:
  - EDITOR: 
  - PATH: 
  - XDG_CONFIG_HOME: "${HOME}/.config"

interpreter: /bin/sh

startup:
  - dunst
  - picom --backend glx

scripts:
  - |
    hour=$(date +%H)
    echo "${hour}"

  - |
    echo "hello"
